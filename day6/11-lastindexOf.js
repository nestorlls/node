const arr = [3, 4, 5, 6, 1, 8, 7, 5, 1];
console.log(arr);

// find last index of 5
const index_of_five = arr.lastIndexOf(5);
console.log('Index of 5 is: %d', index_of_five);

// find last index of 1
const index_of_one = arr.lastIndexOf(1);
console.log('Index of 1 is: %d', index_of_one);

// find last incex of 10
const index_of_ten = arr.lastIndexOf(10);
console.log('Index of 10 is: %d', index_of_ten);
