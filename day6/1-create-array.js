// TODO:An array of names
const names = ['jr', 'ricky', 'alex'];
console.log(names);

// TODO: calculating the length of array
const len = names.length;
console.log(len);

// TODO: Another way to create an Array
let arr = new Array(3);
arr = [1, 2, 3];

// OR
// Declare and initialize in single statement
const arr1 = new Array(2, 3, 4);
console.log(`arr: ${arr}`);
console.log(`arr1: ${arr1}`);
